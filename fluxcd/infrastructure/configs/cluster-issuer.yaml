apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt
spec:
  acme:
    email: timothee.ren@proton.me
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeyRef:
      name: letsencrypt-key
    solvers:
      - http01:
          ingress:
            class: traefik
