apiVersion: apps/v1
kind: Deployment
metadata:
  name: operator
  namespace: desktops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: operator
  template:
    metadata:
      labels:
        app: operator
    spec:
      serviceAccountName: operator
      containers:
        - name: operator
          image: timotheeren/kubepods-operator:sha-6a01f3122e03db5b085d128be8885ccb5520e4d1
