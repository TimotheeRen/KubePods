apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: desktops-postgres-cluster
  namespace: default
  annotations:
    kustomize.toolkit.fluxcd.io/depends-on: default/atlas-operator
spec:
  instances: 1
  storage:
    size: 1Gi
